[project]
name = "crypto-arb-scanner"
version = "0.1.0"
description = "Zero-Token Programmatic Crypto Arbitrage Scanner with 10-Min Action"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[tool.uv]
package = false
